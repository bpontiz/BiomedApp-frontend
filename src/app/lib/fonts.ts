import { IBM_Plex_Sans_Condensed, Sen, Ubuntu } from 'next/font/google';

export const ibm = IBM_Plex_Sans_Condensed({ subsets: ['latin'], weight: ['200', '400', '600']});

export const sen = Sen({ subsets: ['latin'], weight: ['400', '600']});

export const ubuntu = Ubuntu({ subsets: ['latin'], weight: ['300', '400', '500']});